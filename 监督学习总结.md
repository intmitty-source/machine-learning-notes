---
title: 监督学习总结
created: 2026-05-20
tags:
  - machine-learning
  - supervised-learning
  - summary
source: 基于 Clippings/监督学习 目录下菜鸟教程系列文章整理
---

# 监督学习总结

## 一、概述

监督学习是机器学习的核心范式之一，其核心思想是：**利用带标签的数据（输入-输出对）学习一个从输入到输出的映射函数**，从而对新的、未见过的数据做出预测。

根据任务目标，监督学习主要分为两大类：

| 任务类型 | 输出 | 典型场景 |
|---------|------|---------|
| **回归** | 连续数值 | 房价预测、气温预测、销量预测 |
| **分类** | 离散类别标签 | 垃圾邮件识别、疾病诊断、图像分类 |

---

## 二、回归算法

### 2.1 线性回归（Linear Regression）

**核心思想**：假设目标值 `y` 与特征 `x` 之间存在线性关系，找到最佳拟合直线 `y = wx + b`。

**求解方法**：
- **最小二乘法**：通过矩阵公式 `β = (XᵀX)⁻¹XᵀY` 直接求解闭合解。
- **梯度下降法**：迭代更新参数 `w := w - α·∂J/∂w`，逐步最小化损失函数 MSE。

**评估指标**：使用 R² 分数衡量模型解释力。

> 📎 详见 [[Clippings/监督学习/线性回归 (Linear Regression)  菜鸟教程]]

### 2.2 多元线性回归（Multiple Linear Regression）

**核心思想**：线性回归的自然延伸，同时分析 **多个自变量** 对一个因变量的影响。

**模型公式**：`y = β₀ + β₁x₁ + β₂x₂ + ... + βₙxₙ + ε`

**关键假设**：
1. 线性关系
2. 观测值相互独立
3. 同方差性（误差方差恒定）
4. 误差服从正态分布
5. 无多重共线性

**常见挑战**：

| 挑战 | 后果 | 应对方法 |
|------|------|---------|
| 多重共线性 | 系数不稳定 | PCA 降维、正则化（岭回归） |
| 过拟合 | 测试集表现差 | 更多数据、特征选择、正则化 |
| 非线性关系 | R² 低 | 特征变换（多项式、对数）、改用非线性模型 |

> 📎 详见 [[Clippings/监督学习/多元线性回归  菜鸟教程]]

### 2.3 多项式回归（Polynomial Regression）

**核心思想**：为原始特征添加高次项（x², x³...），将非线性关系转化为线性模型处理。本质上，`y = w₁x + w₂x² + b` 仍是关于特征 (x, x²) 的线性模型。

**关键概念**：
- **阶数选择**：阶数太低 → 欠拟合；阶数太高 → 过拟合
- **交叉验证**：选择在验证集上性能最好的阶数

> 📎 详见 [[Clippings/监督学习/多项式回归  菜鸟教程]]

---

## 三、分类算法

### 3.1 逻辑回归（Logistic Regression）

**核心思想**：虽然名为"回归"，实际上是 **二分类算法**。通过 Sigmoid 函数将线性回归的输出映射到 [0, 1] 区间，表示属于某一类别的概率。

**损失函数**：对数损失（Log Loss），使用梯度下降法优化。

**应用场景**：垃圾邮件检测、疾病预测、客户流失预测。

> 📎 详见 [[Clippings/监督学习/逻辑回归（Logistic Regression）  菜鸟教程]]

### 3.2 决策树（Decision Tree）

**核心思想**：通过树状结构进行决策。每个内部节点对一个特征进行测试，每个叶节点代表一个类别或值。

**构建标准**：
- **信息增益（Information Gain）**：基于熵，衡量分割后纯度提升
- **基尼指数（Gini Index）**：衡量数据集不纯度，越小越纯净
- **均方误差（MSE）**：用于回归问题

**优缺点**：
- ✅ 易理解、可处理多种数据类型、无需数据标准化
- ❌ 容易过拟合、对噪声敏感、数据小变化可能导致完全不同的树

> 📎 详见 [[Clippings/监督学习/决策树（Decision Tree）  菜鸟教程]]

### 3.3 支持向量机（SVM）

**核心思想**：找到一个最优 **超平面**，使得不同类别之间的 **间隔（margin）最大化**。

**关键概念**：
- **支持向量**：离超平面最近的样本点，决定了超平面的位置
- **核技巧（Kernel Trick）**：通过核函数将数据映射到高维空间，解决非线性可分问题
  - 线性核、多项式核、RBF 核等

**应用场景**：文本分类、人脸识别、高维小样本数据。

> 📎 详见 [[Clippings/监督学习/支持向量机  菜鸟教程]]

### 3.4 K 近邻算法（KNN）

**核心思想**：**懒惰学习**，无需显式训练。预测时计算待分类样本与所有训练样本的距离，取最近的 K 个邻居进行投票（分类）或平均（回归）。

**特点**：
- ✅ 简单易用、无需训练、适用于多分类
- ❌ 预测时计算量大、对噪声敏感、K 值选择困难

**距离度量**：欧氏距离、曼哈顿距离等。

> 📎 详见 [[Clippings/监督学习/K 近邻算法  菜鸟教程]]

### 3.5 朴素贝叶斯（Naive Bayes）

**核心思想**：基于 **贝叶斯定理** 的概率分类器。核心假设：**所有特征相互条件独立**。

**贝叶斯公式**：`P(A|B) = P(B|A) · P(A) / P(B)`

**三种变体**：

| 类型 | 适用数据 | 典型场景 |
|------|---------|---------|
| 高斯朴素贝叶斯 | 连续型 | 身高体重分类 |
| 多项式朴素贝叶斯 | 离散计数 | 文本分类、垃圾邮件过滤 |
| 伯努利朴素贝叶斯 | 二值型 | 词集模型、用户行为分析 |

**注意事项**：零概率问题可通过 **拉普拉斯平滑**（`alpha` 参数）解决。

> 📎 详见 [[Clippings/监督学习/朴素贝叶斯  菜鸟教程]]

---

## 四、集成学习方法

### 4.1 Bagging（Bootstrap Aggregating）

**核心思想**：有放回随机采样生成多个子训练集，分别训练基学习器，通过 **投票/平均** 聚合结果。目标是 **减少方差**。

**代表算法**：随机森林

### 4.2 Boosting

**核心思想**：**序列化训练**，每个模型纠正前一个模型的错误，通过 **加权投票** 聚合。目标是 **减少偏差**。

**代表算法**：AdaBoost、GBDT、XGBoost、LightGBM

### 4.3 Stacking

**核心思想**：训练多个不同类型的基学习器，将其输出作为新特征，再训练一个 **元学习器** 做最终预测。

> 📎 详见 [[Clippings/监督学习/集成学习  菜鸟教程]]

### 4.4 随机森林（Random Forest）

**核心思想**：Bagging 的经典实现，构建大量决策树，引入双重随机性：
1. **样本随机**：Bootstrap 有放回采样
2. **特征随机**：每个节点随机选择特征子集

**关键超参数**：

| 参数 | 含义 | 通俗解释 |
|------|------|---------|
| `n_estimators` | 树的数量 | 委员会人数 |
| `max_depth` | 最大深度 | 限制发言时间 |
| `max_features` | 分裂时考虑的特征数 | 每次只看几个方面 |
| `min_samples_split` | 节点分裂最小样本数 | 至少几人才能继续讨论 |

**额外能力**：内置 **特征重要性评估**，可用于特征筛选。

> 📎 详见 [[Clippings/监督学习/随机森林  菜鸟教程]]

---

## 五、模型评估

### 5.1 回归评估指标

| 指标 | 公式 | 特点 |
|------|------|------|
| **MAE**（平均绝对误差） | `(1/n)Σ\|y - ŷ\|` | 直观易懂，对异常值不敏感 |
| **MSE**（均方误差） | `(1/n)Σ(y - ŷ)²` | 数学性质好，对大误差敏感 |
| **RMSE**（均方根误差） | `√MSE` | 与原始数据同量纲，最常用 |
| **R²**（决定系数） | `1 - 模型误差/基线误差` | 无量纲，衡量模型相对基线的提升 |

**选择建议**：首选 RMSE + R² 组合；异常值多时关注 MAE；训练时用 MSE 作损失函数。

> 📎 详见 [[Clippings/监督学习/回归模型评估  菜鸟教程]]

### 5.2 分类评估指标

**混淆矩阵**是所有分类指标的基础：

| | 预测正 | 预测负 |
|---|---|---|
| **实际正** | TP（真正例） | FN（假负例 / 漏诊） |
| **实际负** | FP（假正例 / 误报） | TN（真负例） |

**核心指标**：

| 指标 | 公式 | 关注点 | 适用场景 |
|------|------|-------|---------|
| **准确率** | (TP+TN)/总数 | 整体正确率 | 类别均衡时 |
| **精确率** | TP/(TP+FP) | 预测为正的可靠性 | FP 代价高（如推荐系统） |
| **召回率** | TP/(TP+FN) | 找出正例的能力 | FN 代价高（如疾病筛查） |
| **F1 分数** | 2·P·R/(P+R) | 精确率与召回率的平衡 | 类别不平衡时 |

**进阶指标**：
- **ROC 曲线**：以 FPR 为横轴、TPR 为纵轴，展示不同阈值下的性能
- **AUC**：ROC 曲线下面积，对类别不平衡不敏感。AUC=1 完美，AUC=0.5 等同随机猜测

**多分类扩展**：宏平均（平等看待每个类别）、微平均（平等看待每个样本）

> 📎 详见 [[Clippings/监督学习/分类指标  菜鸟教程]]

---

## 六、算法选择速查

| 场景 | 推荐算法 | 理由 |
|------|---------|------|
| 简单回归、基线模型 | 线性回归 | 可解释性强、训练快 |
| 非线性回归 | 多项式回归 | 灵活拟合曲线 |
| 二分类 | 逻辑回归 | 简单高效、输出概率 |
| 高精度分类/竞赛 | XGBoost / LightGBM | 集成学习，性能顶尖 |
| 文本分类 | 朴素贝叶斯 | 高维数据高效，文本场景效果好 |
| 小样本高维数据 | SVM | 核技巧处理非线性 |
| 需要可解释性 | 决策树 | 结构直观 |
| 通用高精度 | 随机森林 | 抗过拟合、特征重要性 |
| 快速原型 | KNN | 无需训练 |

---

## 七、通用 Python 工作流（scikit-learn）

```python
# 1. 导入库
from sklearn.model_selection import train_test_split
from sklearn.metrics import accuracy_score, r2_score

# 2. 划分数据
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.3, random_state=42)

# 3. 训练模型
model.fit(X_train, y_train)

# 4. 预测与评估
y_pred = model.predict(X_test)
score = accuracy_score(y_test, y_pred)  # 分类用 accuracy_score
# score = r2_score(y_test, y_pred)      # 回归用 r2_score
```

---

*本文档基于 Clippings/监督学习 目录下 13 篇菜鸟教程文章整理而成。*
