---
title: 无监督学习总结
created: 2026-05-20
tags:
  - machine-learning
  - unsupervised-learning
  - summary
source: 基于 Clippings/无监督学习 目录下菜鸟教程系列文章整理
---

# 无监督学习总结

## 一、概述

无监督学习与监督学习的核心区别在于：**只提供特征数据，不提供标签**。算法需要自行从数据中发现模式、结构和关系。

无监督学习的两大核心技术方向：

| 方向 | 目标 | 核心问题 |
|------|------|---------|
| **聚类** | 将相似样本归为一组 | "数据可以分成几类？" |
| **降维** | 减少特征数量，保留关键信息 | "如何用更少的维度表达数据？" |

---

## 二、聚类（Clustering）

### 2.1 核心概念

聚类的目标是将数据集划分为若干 **互不相交的子集（簇）**，使得：
- 同一簇内样本 **相似**（距离近）
- 不同簇之间样本 **不相似**（距离远）

相似度通常通过 **距离** 来衡量（如欧氏距离）。

> 📎 详见 [[Clippings/无监督学习/无监督学习 – 聚类  菜鸟教程]]

### 2.2 K-Means 算法

K-Means 是最经典、最常用的聚类算法。

**算法步骤**：

1. **指定 K 值**：预先确定要分成几类
2. **初始化质心**：随机选取 K 个点作为初始质心
3. **分配样本**：将每个样本分配到距离最近的质心所在的簇
4. **更新质心**：重新计算每个簇内所有样本的均值作为新质心
5. **迭代收敛**：重复步骤 3-4，直到质心不再显著变化

**如何选择 K 值——肘部法则**：

- 随着 K 增大，畸变程度（inertia / SSE）下降
- 当 K 小于真实簇数时，增加 K 带来大幅下降
- 当 K 达到真实簇数后，下降幅度骤减，形成"肘部"
- 肘部对应的 K 值即为较优选择

```python
from sklearn.cluster import KMeans

# 训练
kmeans = KMeans(n_clusters=4, random_state=0, n_init='auto')
y_kmeans = kmeans.fit_predict(X)

# 关键属性
centroids = kmeans.cluster_centers_  # 质心坐标
inertia = kmeans.inertia_            # 畸变程度（SSE）
```

### 2.3 聚类的典型应用场景

| 场景 | 说明 |
|------|------|
| **客户细分** | 根据购买行为划分客户群体，实施精准营销 |
| **图像分割** | 按像素颜色/纹理聚类，分离前景与背景 |
| **异常检测** | 远离所有簇中心的点即为异常 |
| **文档归类** | 对文章自动聚类，发现热点话题 |
| **社交网络分析** | 通过用户关系发现社区/圈子 |

---

## 三、降维（Dimensionality Reduction）

### 3.1 核心概念

降维是将高维数据映射到低维空间的过程，**不是简单丢弃特征，而是信息压缩与提炼**。

**为什么要降维？**

| 原因 | 说明 |
|------|------|
| **可视化** | 降至 2D/3D 后可直观观察数据结构 |
| **提升效率** | 更少维度 → 更小存储、更快训练 |
| **去除噪声** | 剔除冗余和不相关特征 |
| **缓解维度灾难** | 高维空间数据稀疏，模型难以学习 |

> 📎 详见 [[Clippings/无监督学习/无监督学习 – 降维  菜鸟教程]]

### 3.2 PCA（主成分分析）—— 线性降维

PCA 是最经典的线性降维方法，通过寻找 **方差最大的方向** 来构建新的坐标轴（主成分）。

**工作流程**：

1. **中心化**：每个特征减去均值
2. **计算协方差矩阵**：描述特征间相关性
3. **特征值分解**：特征向量 → 主成分方向，特征值 → 方差大小
4. **选择前 k 个主成分**：按特征值从大到小排序
5. **数据投影**：将原始数据乘以投影矩阵，得到低维表示

**关键参数 `n_components`**：
- 设为整数（如 2）：指定目标维度
- 设为小数（如 0.95）：保留累计方差贡献率达到该阈值所需的最少主成分

```python
from sklearn.decomposition import PCA

# 降维到 2 维
pca = PCA(n_components=2)
X_pca = pca.fit_transform(X)

# 关键属性
print(pca.explained_variance_ratio_)  # 各主成分解释的方差比例
# 例: [0.9246, 0.0530] → PC1 保留 92.5%，PC2 保留 5.3%

# 保留 95% 方差，自动选择维度
pca = PCA(n_components=0.95)
```

**优缺点**：
- ✅ 计算高效、原理清晰、能去除线性相关性
- ❌ 线性方法，对非线性结构（如"瑞士卷"）效果差

### 3.3 t-SNE —— 非线性降维（可视化导向）

t-SNE 专注于 **保留数据的局部结构**，是可视化高维数据的首选方法。

**核心思想**：高维空间中距离近的点，降维后也应距离近；高维中不相似的点，降维后应远离。

**关键参数 `perplexity`（困惑度）**：
- 典型值 5~50
- 值小 → 关注局部结构；值大 → 关注全局结构
- 对小数据集建议使用更小的值

```python
from sklearn.manifold import TSNE

tsne = TSNE(n_components=2, perplexity=30, random_state=42)
X_tsne = tsne.fit_transform(X)
```

**优缺点**：
- ✅ 非线性数据可视化效果极佳，能清晰展现聚类结构
- ❌ 计算慢、结果随机、超参数敏感、**主要用于可视化而非特征工程**

### 3.4 PCA vs t-SNE 对比

| 维度 | PCA | t-SNE |
|------|-----|-------|
| **类型** | 线性 | 非线性 |
| **目标** | 最大化方差 | 保留局部相似性 |
| **速度** | 快 | 慢 |
| **结果稳定性** | 确定性 | 随机，每次可能不同 |
| **适用场景** | 预处理、特征工程 | 可视化、探索性分析 |
| **可扩展性** | 大数据集友好 | 不适合大数据集 |
| **非线性数据** | 效果差 | 效果好 |

### 3.5 实用组合：PCA + t-SNE

对于高维大数据集（如 MNIST 784 维），推荐先用 PCA 降到中等维度（如 50 维）去除噪声，再用 t-SNE 降到 2 维进行可视化：

```python
# 先 PCA 压缩
pca = PCA(n_components=50)
X_pca = pca.fit_transform(X_mnist)

# 再 t-SNE 可视化
tsne = TSNE(n_components=2, perplexity=40, random_state=42)
X_tsne = tsne.fit_transform(X_pca)
```

---

## 四、算法选择速查

| 目标 | 推荐方法 | 理由 |
|------|---------|------|
| 数据分群/客户细分 | K-Means | 简单高效，最常用 |
| 不知道分几类 | K-Means + 肘部法则 | 自动确定 K 值 |
| 高维数据预处理 | PCA | 线性降维，速度快 |
| 高维数据可视化 | t-SNE | 非线性，可视化效果好 |
| 大数据集可视化 | PCA → t-SNE | PCA 先去噪，t-SNE 再可视化 |
| 去除特征冗余 | PCA | 通过方差贡献率自动筛选 |

---

## 五、通用 Python 工作流（scikit-learn）

### 聚类流程

```python
from sklearn.cluster import KMeans
from sklearn.datasets import make_blobs

# 1. 准备数据
X, _ = make_blobs(n_samples=300, centers=4, random_state=0)

# 2. 肘部法则选 K
inertias = [KMeans(n_clusters=k, n_init='auto').fit(X).inertia_ for k in range(1, 11)]

# 3. 训练最终模型
kmeans = KMeans(n_clusters=4, n_init='auto', random_state=0)
labels = kmeans.fit_predict(X)

# 4. 可视化
import matplotlib.pyplot as plt
plt.scatter(X[:, 0], X[:, 1], c=labels, cmap='viridis')
plt.scatter(kmeans.cluster_centers_[:, 0], kmeans.cluster_centers_[:, 1],
            c='red', marker='X', s=200)
plt.show()
```

### 降维流程

```python
from sklearn.decomposition import PCA
from sklearn.manifold import TSNE

# PCA 降维
pca = PCA(n_components=2)       # 或 n_components=0.95
X_pca = pca.fit_transform(X)
print("方差解释比:", pca.explained_variance_ratio_)

# t-SNE 降维
tsne = TSNE(n_components=2, perplexity=30, random_state=42)
X_tsne = tsne.fit_transform(X)
```

---

## 六、与监督学习的关联

无监督学习在实际工程中常与监督学习配合使用：

1. **特征工程**：用 PCA 降维后的特征作为监督学习模型的输入，减少过拟合、加速训练
2. **数据预处理**：用聚类发现异常点并清洗数据
3. **半监督学习**：少量有标签数据 + 聚类/降维辅助，降低成本
4. **探索性分析**：在建模前先用 t-SNE 可视化数据结构，指导算法选择

---

*本文档基于 Clippings/无监督学习 目录下 2 篇菜鸟教程文章整理而成。*
